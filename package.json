{
  "name": "web-state-inspector",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Chrome DevTools panel for inspecting browser storage.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "verify": "pnpm run typecheck && pnpm run build && pnpm run test"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.21",
    "typescript": "^5.9.2"
  }
}
